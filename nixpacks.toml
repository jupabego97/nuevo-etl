[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install --upgrade pip setuptools wheel",
  "pip install ."
]

[start]
cmd = "uvicorn alegra_etl.web.app:create_app --factory --host 0.0.0.0 --port $PORT"
