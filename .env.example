# PostgreSQL — elige una de las dos estrategias documentadas en README.md
#
# Opción A (recomendada producción): BD dedicada nueva
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/nuevo_etl_prod
#
# Opción B (transición): misma instancia, esquema aislado
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/railway
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/nuevo_etl
DB_SCHEMA=alegra_etl
COMPANY_ID=1

# Alegra API (usar uno de los dos métodos)
ALEGRA_EMAIL=your-email@example.com
ALEGRA_TOKEN=your-token
# ALEGRA_API_KEY=base64-encoded-key
ALEGRA_BASE_URL=https://api.alegra.com/api/v1

# Sincronización
SYNC_OVERLAP_DAYS=7
SYNC_PAGE_SIZE=30
SYNC_REQUEST_TIMEOUT_SECONDS=30

# Webhooks
WEBHOOK_SECRET=change-me-to-a-long-random-secret
WEBHOOK_HOST=0.0.0.0
WEBHOOK_PORT=8000

# Logging y alertas
LOG_LEVEL=INFO
LOG_JSON=true
# ALERT_WEBHOOK_URL=https://hooks.example.com/alerts

# Feature flags (recursos opcionales por plan/país)
ENABLE_ACCOUNTING=true
ENABLE_BANKS=true
ENABLE_GLOBAL_INVOICES=false
ENABLE_TRANSPORTATION_RECEIPTS=false
